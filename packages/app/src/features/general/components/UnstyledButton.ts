import styled from "styled-components";

export const UnstyledButton = styled.button`
  border: none;
  font: inherit;
  color: inherit;
  background: transparent;
  cursor: pointer;
  padding: 0;
`;
